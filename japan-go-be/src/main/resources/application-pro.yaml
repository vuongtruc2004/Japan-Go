spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost:3306}/${DB_SCHEMA:japan-go}?createDatabaseIfNotExist=true
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:1234}

  data:
    redis:
      host: localhost
      port: 6379
      password: ""
      database: 0

sources:
  uri: file:///D:/Ki_8/JPD326/Code/Japan-Go/sources